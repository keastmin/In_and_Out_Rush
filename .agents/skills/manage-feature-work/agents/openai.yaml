interface:
  display_name: "Manage Feature Work"
  short_description: "Check conflicts and verify shared reservations"
  default_prompt: "Use $manage-feature-work to check synchronization, reserve this task, and verify the pushed reservation before implementation."
